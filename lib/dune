;lib/dune

(library
 (name parser)
 (modules parser)
 (libraries ast))

(library
  (name analyzer)
  (modules lexer analyzer)
  (libraries parser)
)

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
